# Namespace Migration Note

Vectrix package root changed from `org.vectrix` to `org.dynamisengine.vectrix`.

Impact:
- benchmark class IDs changed accordingly
- historical benchmark artifacts remain valid, but old class names differ from current ones
